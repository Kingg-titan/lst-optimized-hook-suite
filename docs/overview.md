# Overview

LST-Optimized Hook Suite is a Uniswap v4 specialized-market primitive for LST pairs where rebasing behavior is handled explicitly at hook-time.

Key outcomes:
- deterministic index-aware behavior
- bounded swap conditions around rebase windows
- O(1) yield accounting and split tracking
