# Frontend

The frontend (`/frontend`) is a Vite app for operational visibility:
- connect wallet
- trigger mock rebase
- inspect controller accounting
- inspect hook guardrail runtime state
- evaluate amount checks against active regime

Run:
```bash
npm run --workspace frontend dev
```
